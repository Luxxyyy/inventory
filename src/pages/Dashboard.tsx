import MapComponent from "../components/MapComponent";

function Dashboard() {
    return (
        <div id="mapContainer">
            <div id="mapBox">
                <MapComponent />
            </div>
        </div>
    );
}

export default Dashboard;
